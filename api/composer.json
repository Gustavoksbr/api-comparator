{
    "name": "api-comparator/api",
    "description": "API de cursos (REST, GraphQL e SOAP) em PHP puro, sem dependências externas.",
    "type": "project",
    "require": {
        "php": ">=8.1"
    },
    "autoload": {
        "psr-4": {
            "App\\": "src/"
        }
    }
}
